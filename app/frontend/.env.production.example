# Variable de entorno para el proyecto en Vercel/Netlify. No se lee automáticamente: cárguela
# en el dashboard del proveedor (Vercel: Settings > Environment Variables).

# URL pública del backend en Render (sin slash final), con el prefijo /api.
VITE_API_URL="https://control-servicio-backend.onrender.com/api"
